import {describe,expect,it} from "vitest";import {readFileSync} from "node:fs";const schema=readFileSync("packages/db/prisma/schema.prisma","utf8");
describe("database invariants",()=>{it("deduplicates external accounts and entitlements",()=>{expect(schema).toContain("@@unique([provider, externalAccountHash])");expect(schema).toContain("@@unique([connectionId, externalProductId, accessType])")});it("enforces one reservation per user, release, and edition",()=>{expect(schema).toContain("@@unique([userId, releaseId, editionType])")});it("tracks reservation history, idempotency, and outbox records",()=>{expect(schema).toContain("model ReservationHistory");expect(schema).toContain("model ReservationCommand");expect(schema).toContain("idempotencyKey String @unique");expect(schema).toContain("model OutboxEvent")});it("requires review before evidence verification",()=>{expect(schema).toContain("EvidenceValidationStatus");expect(schema).toContain("PENDING_REVIEW");expect(schema).toContain("reviewedBy")});it("stores local role assignments",()=>{expect(schema).toContain("enum UserRole");expect(schema).toContain("role         UserRole")})});
