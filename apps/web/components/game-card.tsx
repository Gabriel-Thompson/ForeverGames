import Link from "next/link";import type {Game} from "@/lib/demo-data";
export function GameCard({game}:{game:Game}){return <Link className="game-card" href={`/games/${game.slug}`}><div className="cover" style={{"--cover":game.cover,backgroundSize:"cover",backgroundPosition:"center"} as React.CSSProperties}><div className="cover-title">{game.title}</div></div><div className="cardbody"><h3>{game.title}</h3><div className="meta"><span>{game.year||"—"}</span><span>{game.platforms.join(" · ")}</span></div><div className="badges"><span className={`badge ${game.verified?"verified":"digital"}`}>{game.verified?"● Verified":"Imported"}</span><span className={`badge ${game.physical==="PHYSICAL_AVAILABLE"?"physical":"digital"}`}>{game.physical==="PHYSICAL_AVAILABLE"?"Physical exists":"Digital only"}</span>{game.reserved&&<span className="badge reserved">Reserved</span>}</div></div></Link>}
