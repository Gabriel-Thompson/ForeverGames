import Link from "next/link";import type {Game} from "@/lib/demo-data";
export function GameCard({game}:{game:Game}){const label=game.verification==="VERIFIED_PROVIDER"?"● Provider verified":game.verification==="VERIFIED_EVIDENCE"?"● Evidence verified":"Imported · unverified";return <Link className="game-card" href={`/games/${game.slug}`}><div className="cover" style={{"--cover":game.cover,backgroundSize:"cover",backgroundPosition:"center"} as React.CSSProperties}><div className="cover-title">{game.title}</div></div><div className="cardbody"><h3>{game.title}</h3><div className="meta"><span>{game.year||"—"}</span><span>{game.platforms.join(" · ")}</span></div><div className="badges"><span className={`badge ${game.verified?"verified":"digital"}`}>{label}</span><span className={`badge ${game.physical==="PHYSICAL_AVAILABLE"?"physical":"digital"}`}>{game.physical==="PHYSICAL_AVAILABLE"?"Physical exists":game.physical==="DIGITAL_ONLY"?"Digital only":"Physical unknown"}</span>{game.reserved&&<span className="badge reserved">Reserved</span>}</div></div></Link>}
