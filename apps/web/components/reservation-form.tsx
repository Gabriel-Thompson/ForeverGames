"use client";import {useState} from "react";import {CheckCircle2} from "lucide-react";
const disclosure="This request records your interest in an authorized physical edition. It is not a purchase, preorder, deposit, guarantee of production, or transfer of rights. Forever Games may share aggregated, de-identified demand with publishers and platform holders as described in the Privacy Notice.";
export function ReservationForm({title,slug,initial=false}:{title:string;slug:string;initial?:boolean}){const [saved,setSaved]=useState(initial);const [ack,setAck]=useState(false);const [busy,setBusy]=useState(false);const [error,setError]=useState("");async function cancel(){setBusy(true);await fetch(`/api/v1/reservations/${slug}/standard`,{method:"DELETE"});setSaved(false);setBusy(false)}if(saved)return <div className="panel" style={{borderColor:"#a9d7c8"}}><h2><CheckCircle2 size={20} color="#2b8a66"/> Interest recorded</h2><p>Your non-binding request for <b>{title}</b> is active and persisted when PostgreSQL is connected.</p><div style={{display:"flex",gap:8}}><button className="btn btn-secondary" onClick={()=>setSaved(false)}>Edit preferences</button><button className="btn btn-danger" disabled={busy} onClick={cancel}>Cancel request</button></div></div>;return <form className="panel" onSubmit={async e=>{e.preventDefault();setBusy(true);setError("");const form=new FormData(e.currentTarget);const response=await fetch(`/api/v1/reservations/${slug}/standard`,{method:"PUT",headers:{"content-type":"application/json","idempotency-key":crypto.randomUUID()},body:JSON.stringify({targetPriceBand:form.get("price"),shippingCountry:form.get("region"),platformPreference:form.get("platform"),disclosureVersion:"2026-07-13.1"})});if(response.ok)setSaved(true);else setError("The request could not be saved.");setBusy(false)}}><h2>I want a physical edition</h2><div className="form-grid"><div className="field"><label htmlFor="edition">Preferred edition</label><select id="edition" name="edition" required><option value="STANDARD">Standard</option><option value="STEELBOOK">Steelbook</option><option value="COLLECTOR">Collector</option></select></div><div className="field"><label htmlFor="price">Target price</label><select id="price" name="price" required><option value="USD_30_39">USD $30–39</option><option value="USD_40_59">USD $40–59</option><option value="USD_60_79">USD $60–79</option><option value="UNKNOWN">Tell me when pricing is known</option></select></div><div className="field"><label htmlFor="platform">Platform preference</label><select id="platform" name="platform"><option>PC</option><option>PS5</option><option>XBOX_SERIES</option><option>SWITCH</option></select></div><div className="field"><label htmlFor="region">Shipping region</label><select id="region" name="region" required><option value="US">United States</option><option value="CA">Canada</option><option value="GB">United Kingdom</option><option value="EU">European Union</option></select></div></div><p className="notice" style={{marginTop:18}}>{disclosure}</p><label style={{display:"flex",gap:10,alignItems:"flex-start",margin:"16px 0",fontSize:13}}><input type="checkbox" required checked={ack} onChange={e=>setAck(e.target.checked)}/>I understand and acknowledge this disclosure (version 2026-07-13.1).</label>{error&&<p role="alert" style={{color:"var(--red)"}}>{error}</p>}<button className="btn btn-primary" disabled={!ack||busy}>{busy?"Saving…":"Record my interest"}</button></form>}
