"use client";import {useEffect,useState} from "react";import Link from "next/link";import {LogOut,UserRound} from "lucide-react";
export function AccountMenu(){const [user,setUser]=useState<{displayName:string;email:string}|null>(null);useEffect(()=>{fetch("/api/v1/account").then(r=>r.json()).then(v=>setUser(v.data)).catch(()=>setUser(null))},[]);async function signOut(){await fetch("/api/v1/account",{method:"DELETE"});location.href="/sign-in"}return user?<><span className="badge verified"><UserRound size={12}/>{user.displayName}</span><Link className="btn btn-ghost" href="/settings">Settings</Link><button className="btn btn-ghost" onClick={signOut} title="Sign out"><LogOut size={15}/></button></>:<Link className="btn btn-primary" href="/sign-in">Sign in</Link>}
