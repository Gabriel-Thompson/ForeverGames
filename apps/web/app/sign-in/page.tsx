"use client";import {useState} from "react";import {useRouter} from "next/navigation";import Link from "next/link";import {Header} from "@/components/shell";
export default function SignIn(){const router=useRouter();const [error,setError]=useState("");return <><Header/><main className="section" style={{maxWidth:560}}><div className="eyebrow">Local account access</div><h2>Welcome back.</h2><p className="lead">Local development bypasses email verification. Enter the email of an account already created on this computer.</p><form className="panel" onSubmit={async event=>{event.preventDefault();setError("");const email=new FormData(event.currentTarget).get("email");const response=await fetch("/api/v1/account",{method:"PUT",headers:{"content-type":"application/json"},body:JSON.stringify({email})});if(response.ok){router.push("/library");router.refresh()}else setError("That local account was not found.")}}><div className="field"><label htmlFor="email">Email</label><input id="email" name="email" type="email" required autoComplete="email"/></div>{error&&<p role="alert" style={{color:"var(--red)"}}>{error}</p>}<button className="btn btn-primary" style={{width:"100%",marginTop:18}}>Sign in locally</button><p style={{fontSize:13,textAlign:"center"}}>Need another user? <Link href="/onboarding" style={{color:"var(--blue)"}}>Create a local account</Link></p></form></main></>}
