import {NextResponse} from "next/server";import {db} from "@forever-games/db";export const dynamic="force-dynamic";export async function GET(){try{await Promise.race([db.$queryRaw`SELECT 1`,new Promise((_,reject)=>setTimeout(()=>reject(new Error("timeout")),2000))]);return NextResponse.json({status:"ready"},{headers:{"cache-control":"no-store"}})}catch{return NextResponse.json({status:"not_ready"},{status:503,headers:{"cache-control":"no-store"}})}}
