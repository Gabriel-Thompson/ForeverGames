import Link from "next/link";import {PortalShell} from "@/components/shell";import {getReservations} from "@/lib/repository";
export const dynamic="force-dynamic";export default async function Reservations(){const {rows,source}=await getReservations();return <PortalShell><div className="pagehead"><div><h1>Physical requests</h1><p>Non-binding expressions of interest · {source}.</p></div></div><div className="notice">A reservation is not a preorder, deposit, guarantee of production, or transfer of rights. You can edit or cancel at any time.</div><div className="panel"><div className="table-wrap"><table><thead><tr><th>Game</th><th>Edition</th><th>Target price</th><th>Region</th><th>Status</th><th></th></tr></thead><tbody>{rows.map(row=><tr key={`${row.slug}-${row.edition}`}><td><b>{row.title}</b></td><td>{row.edition}</td><td>{row.price.replaceAll("_"," ")}</td><td>{row.region}</td><td><span className={`badge ${row.status==="ACTIVE"?"verified":"digital"}`}>{row.status}</span></td><td><Link href={`/games/${row.slug}`}>Manage</Link></td></tr>)}</tbody></table></div></div></PortalShell>}

