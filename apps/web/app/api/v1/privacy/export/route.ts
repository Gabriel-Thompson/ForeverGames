import {NextResponse} from "next/server";import {db} from "@forever-games/db";import {requireUser} from "@/lib/session";
export async function GET(){try{const user=await requireUser();const data=await db.user.findUniqueOrThrow({where:{id:user.id},include:{consents:true,connections:{select:{provider:true,status:true,capabilities:true,lastSyncedAt:true,entitlements:{select:{externalProductId:true,accessType:true,verification:true,status:true,firstSeenAt:true,lastSeenAt:true,release:{select:{platform:true,game:{select:{title:true,slug:true}}}}}}}},reservations:{include:{release:{select:{platform:true,game:{select:{title:true,slug:true}}}},history:true}},sessions:{select:{id:true,userAgent:true,createdAt:true,lastActiveAt:true,expiresAt:true,revokedAt:true}}}});return new NextResponse(JSON.stringify({exportedAt:new Date().toISOString(),user:data},null,2),{headers:{"content-type":"application/json","content-disposition":"attachment; filename=forever-games-export.json","cache-control":"no-store"}})}catch{return NextResponse.json({error:"Authentication required"},{status:401})}}
