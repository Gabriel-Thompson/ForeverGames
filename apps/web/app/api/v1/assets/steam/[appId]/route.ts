import {NextResponse} from "next/server";
export async function GET(_request:Request,{params}:{params:Promise<{appId:string}>}){const {appId}=await params;if(!/^\d+$/.test(appId))return NextResponse.json({error:"Invalid asset reference"},{status:400});const upstream=await fetch(`https://cdn.akamai.steamstatic.com/steam/apps/${appId}/header.jpg`,{next:{revalidate:86400}});if(!upstream.ok)return NextResponse.json({error:"Asset unavailable"},{status:upstream.status});return new NextResponse(upstream.body,{headers:{"content-type":upstream.headers.get("content-type")??"image/jpeg","cache-control":"public, max-age=86400, stale-while-revalidate=604800","x-content-type-options":"nosniff"}})}
