import {notFound} from "next/navigation";import {PortalShell} from "@/components/shell";import {ReservationForm} from "@/components/reservation-form";import {getGame} from "@/lib/repository";
export const dynamic="force-dynamic";export default async function GamePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const {game,source}=await getGame(slug);if(!game)notFound();return <PortalShell><div style={{display:"grid",gridTemplateColumns:"minmax(220px,330px) 1fr",gap:34,alignItems:"start"}}><div className="cover" style={{"--cover":game.cover,borderRadius:14,aspectRatio:"3/4"} as React.CSSProperties}><div className="cover-title" style={{fontSize:34}}>{game.title}</div></div><div><div className="eyebrow">{game.genre} · {game.year} · {source}</div><h1 style={{fontSize:46,letterSpacing:"-.045em",margin:"10px 0"}}>{game.title}</h1><p className="lead">{game.description}</p><div className="badges"><span className={`badge ${game.verified?"verified":"digital"}`}>{game.verified?"● Verified provider access":"Imported · unverified"}</span><span className="badge physical">{game.platforms.join(" · ")}</span></div><div className="panel"><h2>My access</h2><div className="table-wrap"><table><thead><tr><th>Source</th><th>Access</th><th>Verification</th><th>Freshness</th></tr></thead><tbody><tr><td>Mock Steam</td><td>{game.access.replaceAll("_"," ")}</td><td>{game.verified?"Provider verified":"Imported evidence"}</td><td>Current local seed</td></tr></tbody></table></div></div><ReservationForm title={game.title} slug={game.slug} initial={game.reserved}/><div className="panel"><h2>Community demand</h2><strong style={{fontSize:32}}>{game.reserved?"1,284":"640"}</strong><p style={{color:"var(--muted)"}}>approved, privacy-safe sample requests · verified and unverified tiers are reported separately</p></div></div></div></PortalShell>}
