import {redirect} from "next/navigation";import {PortalShell} from "@/components/shell";import {EvidenceReview} from "@/components/evidence-review";import {getOperations} from "@/lib/repository";import {requireAdmin} from "@/lib/session";
export const dynamic="force-dynamic";export default async function Admin(){try{await requireAdmin()}catch{redirect("/settings?error=admin_required")}const ops=await getOperations();return <PortalShell><div className="pagehead"><div><h1>Operations console</h1><p>Role-protected local administrator view · {ops.source}.</p></div><span className="badge digital">Administrator</span></div><div className="dashboard-grid"><div className="tile"><label>Active users</label><strong>{ops.users}</strong></div><div className="tile"><label>Sync runs</label><strong>{ops.runs.length}</strong></div><div className="tile"><label>Unmatched</label><strong>{ops.unmatched}</strong></div><div className="tile"><label>Active demand</label><strong>{ops.reservations}</strong></div></div><EvidenceReview/><section className="panel"><h2>Recent sync runs</h2><div className="table-wrap"><table><thead><tr><th>Provider</th><th>Run</th><th>Items</th><th>Mapped</th><th>Status</th><th>Duration</th></tr></thead><tbody>{ops.runs.length?ops.runs.map(run=><tr key={run.id}><td>{run.connection.provider}</td><td>{run.id.slice(0,12)}</td><td>{run.imported}</td><td>{run.mapped}</td><td><span className="badge verified">{run.status}</span></td><td>{run.startedAt&&run.completedAt?`${run.completedAt.getTime()-run.startedAt.getTime()} ms`:"—"}</td></tr>):<tr><td colSpan={6}>No persisted sync runs.</td></tr>}</tbody></table></div></section><section className="panel"><h2>Audit log</h2><div className="table-wrap"><table><thead><tr><th>Time</th><th>Actor</th><th>Action</th><th>Reason</th></tr></thead><tbody>{ops.audits.length?ops.audits.map(log=><tr key={log.id}><td>{log.createdAt.toLocaleString()}</td><td>{log.actorId??"system"}</td><td>{log.action}</td><td>{log.reason??"—"}</td></tr>):<tr><td colSpan={4}>No persisted audit events yet.</td></tr>}</tbody></table></div></section></PortalShell>}
