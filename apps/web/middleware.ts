import {NextRequest,NextResponse} from "next/server";import {checkRateLimit} from "@/lib/rate-limit";
const protectedPrefixes=["/library","/connections","/legacy","/reservations","/settings","/games","/admin"];const stateChanging=new Set(["POST","PUT","PATCH","DELETE"]);
function csp(nonce:string,production:boolean){return [`default-src 'self'`,`base-uri 'self'`,`object-src 'none'`,`frame-ancestors 'none'`,`form-action 'self'`,`script-src 'self' 'nonce-${nonce}' 'strict-dynamic'${production?"":" 'unsafe-eval'"}`,`style-src 'self' 'unsafe-inline'`,`img-src 'self' data: https://cdn.cloudflare.steamstatic.com https://shared.cloudflare.steamstatic.com https://avatars.cloudflare.steamstatic.com https://images-eds-ssl.xboxlive.com`,`font-src 'self' data:`,`connect-src 'self' https://api.steampowered.com https://steamcommunity.com https://xbl.io`,`upgrade-insecure-requests`].join("; ")}
export function middleware(request:NextRequest){const production=process.env.APP_ENV==="production";const host=request.headers.get("host")?.split(":")[0];if(production&&host==="www.myforevergames.com"){const url=request.nextUrl.clone();url.hostname="myforevergames.com";url.protocol="https:";url.port="";return NextResponse.redirect(url,308)}if(protectedPrefixes.some(prefix=>request.nextUrl.pathname===prefix||request.nextUrl.pathname.startsWith(`${prefix}/`))&&!request.cookies.has("fg_session")){const url=new URL("/sign-in",request.url);url.searchParams.set("next",request.nextUrl.pathname);return NextResponse.redirect(url)}if(production&&stateChanging.has(request.method)){const origin=request.headers.get("origin");if(!origin||new URL(origin).origin!==(process.env.PUBLIC_BASE_URL??"https://myforevergames.com"))return NextResponse.json({error:"Invalid request origin"},{status:403})}const client=request.headers.get("x-forwarded-for")?.split(",")[0]?.trim()??"unknown";const limit=request.nextUrl.pathname.startsWith("/api/v1/admin")?60:request.nextUrl.pathname.includes("reservations")?30:request.nextUrl.pathname.includes("account")?15:120;const rate=checkRateLimit(`${client}:${request.method}:${request.nextUrl.pathname}`,limit);if(!rate.allowed)return NextResponse.json({error:"Too many requests"},{status:429,headers:{"retry-after":String(Math.ceil((rate.resetAt-Date.now())/1000))}});const nonce=btoa(crypto.randomUUID());const requestHeaders=new Headers(request.headers);requestHeaders.set("x-request-id",request.headers.get("x-request-id")??crypto.randomUUID());requestHeaders.set("x-nonce",nonce);requestHeaders.set("content-security-policy",csp(nonce,production));const response=NextResponse.next({request:{headers:requestHeaders}});response.headers.set("content-security-policy",csp(nonce,production));response.headers.set("x-content-type-options","nosniff");response.headers.set("referrer-policy","strict-origin-when-cross-origin");response.headers.set("permissions-policy","camera=(), microphone=(), geolocation=(), payment=(), usb=()");response.headers.set("cross-origin-opener-policy","same-origin");response.headers.set("cross-origin-resource-policy","same-origin");response.headers.set("x-request-id",requestHeaders.get("x-request-id")!);if(production)response.headers.set("strict-transport-security","max-age=31536000; includeSubDomains; preload");return response}
export const config={matcher:["/((?!_next/static|_next/image|favicon.ico).*)"]};
